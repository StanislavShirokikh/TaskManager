package org.example;

import org.example.all_tasks.Epic;
import org.example.all_tasks.SubTask;
import org.example.all_tasks.Task;
import org.example.working_on_tasks.IdGenerator;
import org.example.working_on_tasks.InMemoryTaskDao;
import org.example.working_on_tasks.Manager;

public class Main {
    public static void main(String[] args) {
//        IdGenerator generatorId = new IdGenerator();
//        InMemoryTaskDao inMemoryTaskDao = new InMemoryTaskDao();
//        Manager manager = new Manager(inMemoryTaskDao);
//        Task task1 = manager.createTask(new Task("Задача1", "Выполнить задачу1", generatorId.getSequence(),
//                Status.NEW));
//        manager.saveTask(task1);
//
//        Task task2 = manager.createTask(new Task("Задача2", "Выполнить задачу2", generatorId.getSequence(),
//                Status.NEW));
//        manager.saveTask(task2);
//
//        Epic epic1 = manager.createEpic(new Epic("Эпик1", "Выполнить эпик1", generatorId.getSequence()));
//        manager.saveEpic(epic1);
//
//        SubTask subTask1 = manager.createSubTask(new SubTask("Подзадача1", "Выполнить подзадачу1",
//                generatorId.getSequence(), Status.NEW, epic1));
//        manager.saveSubtask(subTask1);
//
//        SubTask subTask2 = manager.createSubTask(new SubTask("Подзадача2", "Выполнить подзадачу2",
//                generatorId.getSequence(), Status.NEW, epic1));
//        manager.saveSubtask(subTask2);
//
//        Epic epic2 = manager.createEpic(new Epic("Эпик2", "Выполнить эпик2", generatorId.getSequence()));
//        manager.saveEpic(epic2);
//
//        SubTask subTask = manager.createSubTask(new SubTask("Подзадача", "Выполнить подзадачу",
//                generatorId.getSequence(), Status.NEW, epic2));
//        manager.saveSubtask(subTask);
    }
}